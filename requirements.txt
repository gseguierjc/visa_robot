# Requisitos mínimos para ejecutar visa_reprogram.py
# Python >= 3.8 recomendado
# Instala con: pip install -r requirements-visa_reprogram.txt

selenium>=4.8.0
requests>=2.28.2
webdriver-manager>=4.0.0  # opcional: facilita la gestión automática del driver de Chrome/Edge

# Notas:
# - mimetypes, pathlib, datetime, typing, io, os, time, subprocess, shutil son parte de la stdlib.
# - Si prefieres no usar webdriver-manager, descarga el chromedriver/edgedriver compatible y ponlo en PATH.
# - Si usas entornos virtuales: python -m venv .venv; .\.venv\Scripts\Activate.ps1; pip install -r requirements-visa_reprogram.txt
